export enum Role {
  STUDENT = 'student',
  ADMIN = 'admin',
  STAFF = 'staff',
}

export enum Category {
  ACADEMIC = 'academic',
  NON_ACADEMIC = 'non-academic',
}

export enum Status {
  PENDING = 'Pending',
  RESOLVED = 'Resolved',
  ESCALATED = 'Escalated',
}