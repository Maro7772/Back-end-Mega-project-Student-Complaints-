export enum Role {
  STUDENT = 'Student',
  ADMIN = 'Admin',
  STAFF = 'Staff',
}

export enum Category {
  ACADEMIC = 'Academic',
  NON_ACADEMIC = 'Non-Academic',
}

export enum Status {
  PENDING = 'Pending',
  RESOLVED = 'Resolved',
  REJECTED = 'Rejected',
}