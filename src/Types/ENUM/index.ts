export enum Role {
  STUDENT = "student",
  ADMIN = "admin",
}

export enum Category {
  ACADEMIC = "Academic",
  NON_ACADEMIC = "Non-Academic",
}

export enum Status {
  PENDING = "Pending",
  RESOLVED = "Resolved",
  REJECTED = "Rejected",
}
